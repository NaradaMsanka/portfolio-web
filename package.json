{
  "name": "aventro-projects-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm --prefix functions start",
    "server": "npm --prefix functions start",
    "firebase:setup": "node scripts/setup-firebase.mjs",
    "admin:hash-password": "npm --prefix functions run admin:hash-password",
    "admin:generate-session-secret": "npm --prefix functions run admin:generate-session-secret",
    "deploy:firestore-rules": "firebase deploy --only firestore:rules"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "firebase": "^12.16.0",
    "lucide-react": "^1.23.0",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
